// TaskSortField enum for sorting tasks
export enum TaskSortField {
  DUE_DATE = 'dueDate',
  UPDATE_DATE = 'updateDate',
  TITLE = 'title',
}

// TaskSortOrder enum for sort order
export enum TaskSortOrder {
  ASC = 'asc',
  DESC = 'desc',
}
